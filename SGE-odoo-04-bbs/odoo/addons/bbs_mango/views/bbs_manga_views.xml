<odoo>
    <record id="view_manga_tree" model="ir.ui.view">
        <field name="name">manga.shop.manga.tree</field>
        <field name="model">manga.shop.manga</field>
        <field name="arch" type="xml">
            <tree>
                <field name="name"/>
                <field name="autor_id"/>
                <field name="editorial_id"/>
                <field name="precio"/>
                <field name="stock"/>
            </tree>
        </field>
    </record>

    <record id="view_manga_form" model="ir.ui.view">
        <field name="name">manga.shop.manga.form</field>
        <field name="model">manga.shop.manga</field>
        <field name="arch" type="xml">
            <form>
                <sheet>
                    <group>
                        <field name="name"/>
                        <field name="precio"/>
                        <field name="stock"/>
                        <field name="fecha_publicacion"/>
                        <field name="genero"/>
                        <field name="autor_id"/>
                        <field name="editorial_id"/>
                        <field name="image_1920" widget="image"/>
                    </group>
                    <notebook>
                        <page string="Pedidos">
                            <field name="pedidos_ids">
                                <tree>
                                    <field
